# AG-04 派工单：创新点前端可用性修复（第四轮）

> 派工方：经用户授权，ZCode 代写（2026-09-08）。依据：`docs/innovation/创新点前端可用性评估-v1.md`（ZC-04 实机走查，截图与现象均已记录）。
> 边界：只改 `frontend/src/` 及前端自身文档；**不改后端、不改脚本、不改论文主稿、不改他人报告**；小步提交（`[前端线]` 前缀）、完成后停在验收点。
> 运行栈已于 09-08 重建至 `cc4eb8a`，改完可本地 `npm run build` 验证，无需自行部署。

## P1（直接影响演示观感，优先）

1. **Agent 助手浮层遮挡页面按钮**：长回答后底部通知气泡（`fixed bottom-5 right-5 z-[60]`）盖住"调整阈值"等按钮。要求：气泡限时自动消失或可一键收起；长回答默认折叠（"展开全文"）；点击外部区域可收起侧栏。位置：`frontend/src/components/shared/AgentAssistant.tsx`。
2. **图谱悬停聚焦命中率低**：节点 10–18px，拾取半径过小，多点采样多数不触发。要求：扩大拾取区域（建议 ≥24px）或让"标签"开启态文字也可命中；补充"点击锁定/释放固定"提示文案。位置：`frontend/src/components/kg-visualization.tsx`（注意保持 AG-01 注明的 `nodePointerAreaPaint` 拾取层设计意图）。
3. **引用校验横幅文案自相矛盾**："引用校验未通过：引用校验通过，共核对 22 个证据编号。 缺少强制引用：……"。要求：拆总体判定与明细两段（如"22 个编号核对通过；1 处关键事实缺强制引用 → 降级 needs_review"）。位置：AI 问答页引用校验横幅渲染处。

## P2（同批处理）

4. **报告页失败记录置顶**：3 条 2026/9/1 历史失败记录排在成功记录前。要求：失败记录后置或默认折叠，加"仅看成功"过滤或失败原因悬停提示。
5. **蓝图缺口 → AI 建议不互通**：在蓝图缺口详情面板加"让 AI 围绕此缺口给建议"入口，打通"缺口→建议→行动"。
6. **蓝图图例文案残留**：`kg-blueprint.tsx` 仍写"实心水波 = 已获实验记录实证""形状=实体角色"。要求改为"实心节点=已实证 / 虚线节点=待实证""颜色=实体类型"，删除"水波/形状"表述。**此项同时是论文口径一致性风险，必须改。**

## 验收方式

- 修完后自行走查：Agent 长回答下"调整阈值"可点；图谱悬停 3 个随机点至少 2 个触发聚焦；横幅文案无自相矛盾；蓝图页无"水波/形状"字样。
- 在本文件末尾追加"执行记录"小节（改了什么/验证方式/未做项），保持只写自己文件纪律。
