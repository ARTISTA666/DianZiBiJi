# 开发启动说明

## 默认技术栈

- 前端：Next.js + TypeScript + Tailwind CSS
- 后端：FastAPI + SQLAlchemy
- 数据库：PostgreSQL
- 部署：Docker Compose

## 首次启动

在 `D:\电子笔记系统` 目录执行：

```powershell
Copy-Item .env.example .env
docker compose up --build
```

启动后访问：

- 前端：http://localhost:3000
- 后端文档：http://localhost:8001/docs

## 默认账号

```text
账号：admin
密码：admin123
```

首次正式部署后需要修改默认密码。

## 当前已完成

- Monorepo 目录结构
- Docker Compose
- FastAPI 后端骨架
- PostgreSQL 数据模型
- 默认管理员种子数据
- JWT 登录
- 登出审计接口
- 用户管理接口
- 用户详情、更新和停用接口
- 小组管理接口
- 小组详情、更新和成员管理接口
- 项目管理接口
- 项目详情和更新接口
- 项目成员授权接口
- 项目成员更新、移除和指定审核人接口
- 实验笔记草稿 CRUD
- 笔记版本记录
- 单个版本读取
- 笔记提交、审批、退回、归档和作废
- 审批记录查询
- 5 个内置实验模板
- 附件上传、下载和下载留痕
- 资料库上传、列表和审核接口
- 审计日志基础表、接口和筛选参数
- Next.js 登录页
- Next.js 工作台首页
- 前端用户创建、编辑、停用界面
- 前端小组创建、编辑和成员维护界面
- 前端项目创建、编辑、成员授权、成员权限调整和审核人设置界面
- 前端笔记编辑、审批中心、附件下载、资料审核基础界面
- 前端笔记列表按关键词、状态、实验类型筛选
- 前端资料/附件按关键词、类型、状态筛选，支持文件名元信息维护、归档，并展示资料审核后的知识库同步占位状态
- 前端审计日志筛选和查看界面
- 前端项目详情分标签视图，覆盖项目概览、实验笔记、资料库、审批中心、成员权限和项目日志
- Docker Compose 环境已验证后端健康检查、前端首页访问和非 AI 主流程 API
- 前端 `npm run lint` 已替换为基于 `tsconfig.check.json` 的 TypeScript 静态检查，避免 `.next/dev` 运行态类型污染

## 下一步

1. 补充后端自动化测试用例，固化已验证的非 AI 主流程。
2. 视需要引入独立 ESLint 规则集；当前 Next 16/ESLint 9 组合在本地执行耗时异常。
