# 项目全景梳理(2026-08-29)

> status: superseded
> owner: 作者/证据线
> superseded-by: docs/项目进展与论文核心思路-2026-09-05.md（全景以 09-05 版为准，遗留清单并入任务台账）

> 目的:论文与系统规模已大,并线开发(证据线 ZCode + 文字线 GPT)启动前,把家底盘清。
> 本文档是遗留清单的**唯一权威版本**;增量记录在 `docs/并线协作状态.md`。

## 一、一句话现状

系统真实、测试全绿、论文三轮优化后"低级硬伤清零";剩余风险集中在三类**需要真实数据/人工**的证据缺口(legacy 批次脱钩、零人工盲评、事后答案规则),当前以诚实限定语兜底。

## 二、资产地图

| 资产 | 位置 | 状态 |
| --- | --- | --- |
| 生产后端(Rust/Axum) | `backend/src/` | 264 项测试全绿(含 3 项安全性质测试);fmt/clippy 干净 |
| legacy Python 后端 | `backend/app/`、`backend/tests/` | 346 通过/11 跳过(SQLite 模式);仅开发参考 |
| 论文正稿 | `docs/毕业论文重构稿.md` | 1898 行,八章+附录 A-D,三轮优化完成 |
| LaTeX 排版 PDF | `docs/毕业论文_AHNU-LaTeX排版.pdf` | **已过期**(落后三轮修改),需重排 |
| 系统证据 | `docs/system-evidence/`(11 件) | 已入库;新增 `agent-safety-property-tests-2026-08-28.md` |
| 实验记录 | `docs/experiments/`(66 件已入库 + 6 件本轮入库) | 含 RAGAS 评估、消融基线、盲评表单 |
| 评审档案 | `reviews/`(smoke-test + round-01..03) | 六评审报告、合并清单、基线快照、edit-report |
| 多 Agent 审核系统 | `agent-work/thesis-agents/`(8 角色)+ `.zcode/commands/thesis-review.md` | 可复用,新会话 `/thesis-review [范围]` |
| 归档区 | `archive/`(8 组 + rendered,62+ 件) | 2026-08-29 归档的过期文档/旧版论文/遗留脚本;git mv 保留历史,零删除,详见 `archive/README.md` |
| 实验工作区 | `agent-work/`(44MB,freeze/runs/question-sets/blind-review 等) | DRAFT/未冻结,保持本地不入库 |
| 数据集 | `data/real/`(129MB,GSE 系列等) | 本地不入库 |

## 三、Git 入库决策(本轮已执行)

- 已入库:论文、测试、证据、评审档案、agent 定义、协作约定(5 个分批提交,均带 `[证据线]` 前缀)。
- 不入库及原因:`data/real/`(129MB)、`agent-work/` 实验材料(44MB DRAFT)、排版 PDF(过期)、`.zcode/plans/`(会话内部)。
- ⚠️ 注意:`docs/experiments/rag-experiment-4-blind-review-key.csv`(盲评解盲键)**已在仓库历史中**;若将来仓库公开需先重写历史剔除。

## 四、论文健康度

- 结构:章节闭环完整,5.2 六小节与导语一致,图 6-6~6-13/表 7-11~7-13 编号连续,围栏 40 条配对,TODO 残留 0,参考文献 [1]-[118] 连续。
- 三个核心创新点:
  - 创新点一(审核门禁流水线):机制真实可查;创新高度被评审质疑为"工程贡献",已有不变式测试可扩展。
  - 创新点二(可审计证据链 + 图谱增强 RAG):式(5-3) 已正式定义;**最大硬伤**——全部质量实验(4→14 等)在 legacy 批次完成,与交付 Rust 系统脱钩,现以 1.5/8.2 限定语兜底。
  - 创新点三(安全运行时):确认队列/幂等防重放已由 3 项集成测试证明,验证缺口**已关闭**。
- 口径:所有强结论已携带 legacy 批次/空载语料/paper_ready=false 限定;实验 4/5 编号已在 7.1 说明。

## 五、遗留工作(按可动性分类)

### A. 需要人/真实数据(作者推动)
1. **人工盲评签核**:40 条回答盲评表 + 解盲键已备好,需 1-2 名独立评价者打分 → 报告一致率/kappa(消除"零独立人工评价"Critical)。
2. **真实用户测试**:3-5 名科研用户走"录入→审核→问答→核对来源"任务集。
3. **预注册冻结重跑**:第三方冻结问题/规则/哈希后重跑(流程见 8.4,未执行)。

### B. 需要作者决策
1. Rust 侧重跑 20 题对照实验(消除 legacy 脱钩根因;可先做不调 LLM 的 retrieval-only 重放探路)。
2. 金标准扩展标注(受限于 11 条笔记无种子数据,只能覆盖有数据部分)。
3. 排版 PDF 重新生成(基于三轮修改后的正稿)。
4. 解盲键入库历史处置(公开前)。

### C. 可继续推进(已入两线队列,见 `docs/并线协作状态.md`)
- 文字线(GPT):各章小结长句、破折号 41 处、"暴露"句式 9 处、"其一/其二"统一(L1422)、摘要双冒号。
- 证据线(ZCode):盲评统计脚本(等 A1)、retrieval-only 重放(等 B1 授权)、金标准扩展脚本(等 B2)。

## 六、Editor 标注的待作者复核点(第 1-3 轮累计,均已可运行但观感/口径请过目)

1. 表 7-2 新增行位置(安全边界测试行之后)。
2. L1215 测试口径句的"SQLite 模式"括注替换。
3. [117]/[118] 引用编号紧随"[0,1]"记号之后的排版。
4. L404 "JWT(JSON Web Token)Token"叠词、L698 NFKC 括注、L118 SOP 括注嵌套过深(**建议交文字线拆句**)。
5. 33 张表清点结果(4.5 节枚举 33,与附录 B 请对照)。
6. 式(5-3) 的 LaTeX 渲染效果(aligned 环境 + \mathbf{1} 记号)。
7. 7.10 首句"建立了……覆盖……六个方面"的措辞。

## 七、建议推进顺序

1. 作者过目第六节 7 项 → 有异议丢回对应线修改。
2. 文字线按队列做纯语言轮(窗口已交);完成后证据线跑一次 `/thesis-review` 快速复审。
3. 作者启动盲评(A1);盲评数据回来后证据线出统计 + 7.7 节改写。
4. 决策 B1(retrieval-only 探路可先行,成本为零)。
