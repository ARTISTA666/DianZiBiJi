# 真实 DeepSeek 最小回归（2026-07-16）

## 结论

真实 DeepSeek 接口的最小生成链路连续两轮均为 3/3 通过。它只证明当前密钥、网络、客户端解析和三个最小行为可用，**不能证明 RAG、Agent 或项目已经成熟，也不能作为人工评审启动依据**。

## 最近一次结果

模型报告值为 `deepseek-v4-flash`，总计 121 个 token。脚本没有输出或保存提示词、模型回答和 API 密钥。

| 用例 | 判定 | 延迟 | token | 请求 ID | 回答 SHA-256 |
| --- | --- | ---: | ---: | --- | --- |
| 精确指令 | 通过 | 713 ms | 25 | 有 | `8e9d202…` |
| 无证据拒答 | 通过 | 542 ms | 37 | 有 | `168efd17…` |
| 单来源引用 | 通过 | 833 ms | 59 | 有 | `34629b07…` |

运行方式：

```bash
backend/.venv/bin/python scripts/validate_real_llm.py
```

## 明确限制

- 用例只有 3 条，使用短提示词，不经过文档解析、实际嵌入、BM25/向量召回、图谱检索或 Agent 工具链。
- 判定仅检查必要字符串，不等同于事实准确率、引用完整性或语义质量评价。
- 429、5xx、传输错误、无效 JSON 和空回答通过确定性客户端测试覆盖；本次真实调用没有主动制造供应商故障。
- 没有执行长时间、批量、限额边界、网络抖动或多用户压力测试。
- SHA-256 仅用于确认输出是否变化；文档使用截断值，完整值只存在于当次终端输出，没有保存回答原文。

在外部冻结的多项目语料、问题和标准事实完成全链路回归前，人工评审继续冻结。
