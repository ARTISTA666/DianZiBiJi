# 创新点零调用补证批次（2026-09-08，ZC-05 / T-024）

> status: current
> owner: ZCode（证据线）
> 依据：`docs/coordination/ZC-03.md` 最小补证方案表四项（①-A / ①-B / ②-C C1 / ②-D D1），用户 2026-09-08 "自由推进、闭环论文素材"授权。
> 纪律：**零 LLM 调用、零源码改动、只读 DB**（唯一写操作：本报告与 `/tmp` 工作文件）；运行栈 `/ready` revision `cc4eb8a`（2026-09-08 重建）。
> 定位：这批证据只填 ZC-03 方案表中"可立即执行"格；**样本量与口径边界逐处标注，均不构成效果主张**。

## ②-D D1：预警四维指标 SQL 重算一致性

**研究问题**（ZC-03 ②-D）：系统预警评估的四维指标计算是否可由 SQL 独立重算复现。

**方法**：按 `backend/src/project_alert.rs::evaluate_project_alerts` 的四条 SQL 逐条在运行库手工重算（脚本：`/tmp/zc05/d1_metric_recompute.sql`，输出快照 `/tmp/zc05/d1_recompute_output.txt`），与 audit_logs 中最后一次 `alerts_evaluate` 留痕（id 928，2026-09-07 05:23:37Z）对照。

**结果**（项目 10，重算时刻 2026-09-08 16:0x CST）：

| 指标 | SQL 重算值（09-08） | 系统留痕值（09-07 05:23） | 一致性判定 |
| --- | --- | --- | --- |
| review_stall_hours | 0（无 SUBMITTED 笔记） | 0.0 | ✓ 一致 |
| return_rate | 0.1 | 0.1 | ✓ 一致 |
| blueprint_stagnant_days | 0.867（09-08 时点，随时间单调增长，符合定义） | 0.016（09-07 时点） | ✓ 定义一致（时间差解释） |
| progress_deviation | 0.9333 | 0.933 | ✓ 一致（0.9333… 留痕截断为 3 位小数） |

判定逻辑核对：默认阈值 progress_deviation warn=0.7 / critical=0.9（代码 `default_thresholds`），0.933 ≥ 0.9 → critical，与留痕 level 一致。

**复算过程中发现的真实缺陷（如实记录，已报 ZC-05）**：audit_logs id 901/903（2026-09-07 04:54）显示导师调整阈值时输入了 **百分数口径（warn 80/critical 95、随后 70/90）**，而指标值为 **0-1 比例口径**，`snapshot_metric` 直接比较 `value >= critical_threshold` → 0.933 < 70 → **判定为 none，告警静默失效**（id 902/914 留痕佐证：value 1.0 / 0.933 时 level=none）。05:01:19→05:01:32 之间阈值行被清除（无删除路由、无删除审计，推断为走查期间 psql 手工清理）后恢复 critical。**缺陷定性：阈值输入缺单位归一化校验（upsert 仅校验 0 ≤ warn ≤ critical，未限制上限）**；修复属源码改动，本轮不改码，登记待授权项。D2（预警效果层）按 ZC-03 口径仍属试用批次协议，本轮无数据。

## ②-C C1：建议→审核的事后追溯命中率（相关，非因果）

**研究问题**（ZC-03 ②-C）：主动建议引用的知识点是否在其后被学生实证覆盖（事后追溯代理指标）。

**数据**：audit_logs 7 条 `agent_next_step_suggestion`（2026-09-07 04:46 起，项目 10，全部 mode=llm）；首条建议时点蓝图 15 节点 0 覆盖（coverage=0.0 留痕）。

**结果**：建议后 14 分钟内（04:46→05:00）学生提交并审核通过 2 条笔记（#45 外泌体 WB 鉴定、#46 WB CD63/TSG101），`auto_extract_note_kg` 留痕 entities 4+12：

| 口径 | 命中定义 | 命中数 | 说明 |
| --- | --- | --- | --- |
| 严格（系统精确匹配） | 蓝图 (类型,规范名) 与实证实体精确相等 | **1/15**（Western Blot，entity 703） | 系统覆盖判定即此口径，coverage 0→0.067 留痕吻合 |
| 宽松（人工语义映射） | 笔记内容实际覆盖蓝图知识点 | **3/15**（Western Blot、CD63、TSG101——后两者实证侧为 anti-CD63/anti-TSG101 抗体与阳性结果，规范名不同未命中系统匹配） | 差异暴露**抽取归一化粒度**问题：试剂"CD63"与"anti-CD63 抗体"未归并 |

**边界（必须与结果同列）**：①**相关非因果**——学生可能本来就计划做 WB（笔记 45/46 的实验在建议前已安排，蓝图节点 6 的实体在 09-07 11:37 已存在）；②样本仅 1 次建议→2 条笔记，**不构成采纳率或效果证据**；③C2 显式采纳留痕（accepted/ignored 按钮）后端表已建（`agent_suggestion_feedback`，migration 0017 + Rust schema v6）但**前端未接线、生产未部署**，属待授权开发项。

## ①-A：蓝图增量解析（留痕分析，二次实测降级）

**计划**（ZC-03 ①-A）：A1 首次解析 vs A2 二次解析，测墙钟耗时与幂等性。
**实际执行受限**：两次解析的输入文本（计划书 v1，inline text 方式）**未落库**（`kg_blueprint_documents` 只存 title/mode/计数，不存原文；audit 同样不含 text）——同文本二次解析无法复现，且运行栈已配 AI key，重发请求必然调用 LLM（违反本轮零调用纪律）。**降级为留痕分析**：

| 解析（2026-09-07） | parse_mode | 新增节点 | 新增边 | 蓝图总量（解析后） |
| --- | --- | --- | --- | --- |
| 第 1 次 04:44:59 | llm | 13 | 12 | 13 节点/12 边 |
| 第 2 次 04:45:23 | llm | 2 | 2 | 15 节点/14 边 |

第二次解析仅新增 2 项（首次漏抽的样本类节点），其余与已有 13 节点合并（无重复节点：同 (type, normalized_label) 唯一，UPPER 唯一索引验证 0 冲突）——**增量合并行为与"推倒重建"区分开的留痕证据**。墙钟耗时未留痕（后端未记 duration_ms），列为待改项。**论文可主张的仅限：三态合并机制存在且留痕显示二次解析只补增量；不能主张耗时数字。**

## ①-B：消融 JSON 引用绑定数据整理（B1/B2 可整；B4 不可行）

**结论先行**：ZC-03 ①-B 设想"B4 = LLM+蓝图输入臂从审计日志补录"经查**不可行**——`agent_next_step_suggestion` 审计 detail 只存统计量（mode/coverage/计数），**建议正文（含 related_labels）只回响应不落库**（agent_events 0 命中）。可整理的只有已有 JSON 的 B1/B2 两臂，数字与消融报告完全一致（未重新计算，仅转录成论文可用表格）：

| 臂 | 输入 | 生成器 | 可执行引用 | 命中待实证 | 编造引用 |
| --- | --- | --- | --- | --- | --- |
| B1 | 蓝图未覆盖清单 | rule_based（系统真实兜底路径） | **3** | 3 | 0 |
| B2 | 无蓝图 | rule_based（通用静态建议） | 0 | 0 | 0 |
| B3 | 笔记列表（无蓝图强基线） | LLM | 未采集 | — | — |
| B4 | 蓝图未覆盖清单 | LLM | 未落库，**无法补录** | — | — |

**论文口径约束**（与消融报告勘误版一致）：只写"引用绑定与覆盖指引"计数对照；分母为零不作比率；不证明建议质量/正确性；B3/B4 是缺口，不隐去。

## Z1：盲评一致性系数（κ/α）——数据未就位，无法计算

两批正式盲评表（`agent-work/blind-review/formal-batch-2026-08-14/` 与 `formal-batch-2026-08-16-5projects/`，共 2×(120+198) 份行）的 `accuracy_rating` / `traceability_rating` **全部为空**（人工尚未填打分）。一致性计算脚本 `analysis/agree.py`（Cohen's κ + 原始一致率）已就位，待人工签核（台账 Q-03）后一条命令可出结果。**本轮不产出任何 κ/α 数字。**

## 汇总：可入论文素材 / 缺口

| 项 | 可入论文 | 仍缺口（登记待授权/待人工） |
| --- | --- | --- |
| ②-D D1 | 四指标 SQL 可复算一致（机制正确性证据）+ 阈值单位缺陷的发现与定性 | 阈值归一化修复（源码）；D2 效果数据（试用） |
| ②-C C1 | 事后追溯两口径命中 1/15 vs 3/15 + 归一化粒度发现 | C2 采纳留痕前端接线；样本量（1 建议→2 笔记） |
| ①-A | 增量合并留痕（13→2→15，零重复节点） | 解析文本落库；耗时 duration_ms 记录；同文本二次解析实测 |
| ①-B | B1/B2 引用绑定计数表（3/0/0 vs 0/0/0） | B3/B4 采集（B4 需先修建议正文落库） |
| Z1 | 无（脚本就位） | 人工盲评打分（Q-03） |

## 可复核性

- 复算 SQL 与输出快照：`/tmp/zc05/`（d1_metric_recompute.sql、d1_recompute_output.txt、c1_exact_match.txt、c1_note_approvals.txt、d2_alert_lifecycle.txt）；入本报告的数字均由上列 SQL 在运行库直查得出，重跑即可复核。
- 审计留痕锚点：audit_logs id 886/887（蓝图解析）、888-942（建议）、899-928（预警评估/阈值/确认）。
- 代码锚点：`backend/src/project_alert.rs`（指标 SQL 与判定）、`backend/src/kg_blueprint.rs`（三态合并）、`backend/src/api/agent_suggestion.rs`（建议与留痕）。
