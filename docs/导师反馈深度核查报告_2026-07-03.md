# 导师反馈深度核查报告

核查日期:2026-07-03

核查对象:

- `D:\new\论文导师反馈汇总.md`
- `D:\new\full-system\docs\毕业论文初稿.md`
- `D:\new\full-system\docs\论文反馈修订对照表.md`
- `D:\new\full-system\docs\论文成稿检查清单.md`
- `D:\new\full-system\docs\审核意见\20260615-逐条回复与修改对照表.md`
- `D:\new\full-system\docs\论文对抗式自审_20260615.md`
- `D:\new\full-system\backend`
- `D:\new\full-system\frontend`
- `D:\new\full-system\docs\experiments`
- `D:\new\full-system\docs\user-guide-assets`

## 一、总体结论

按 `论文导师反馈汇总.md` 这份原始导师意见核查,除独立人工盲评、真实多项目数据、学校封面身份信息、亲笔签名和查重前降重复外,当前能由项目和论文作者完成的工作已经基本完成。

本次深度核查没有只看“已完成”状态,而是交叉检查了正文、修订对照表、实验附件、截图资产、代码实现和测试结果。核查过程中发现并修正了三类可立即处理的问题:

1. 正文第七章仍写后端测试为 `29 passed`,已修正为当前实测 `105 passed`。
2. Q15 仍以“当前配置 top-10 不可完成”表述,但项目代码已新增集合型问题上下文上限放宽。已改为“原实验批次暴露该问题,项目代码已修正,但不能回填为原实验结果,需新批次验证”。
3. 第三章仍保留“问题建模”标题,且第四至七章部分系统细节仍偏概述。已将第三章改为“问题定义与设计约束提炼”,并在正文补入需求证据链、图谱处理链路、DeepSeek 调用细节、功能模块映射和实验材料处理路径。

因此当前状态不是“所有研究缺口都消失”,而是:

- 导师原始意见中要求的结构、设计、实现、截图、数据库、图谱、API、测试和实验材料已补齐。
- 后续盲审指出的研究有效性缺口仍存在,但这些缺口需要人工评价、真实数据或新实验协议,不能用写作或代码伪造。

## 二、逐项核查结果

| 导师反馈项 | 当前状态 | 核查证据 | 判断 |
| --- | --- | --- | --- |
| 第三章需求分析定位不清 | 已处理 | 正文第 3 章标题改为“需求分析与问题定义”;3.6 将四类问题落实为需求约束、后续处理方式和可核验材料 | 完成 |
| 第四、五章设计内容不足 | 已处理 | 第 4 章补入图谱抽取输入、处理规则、输出对象和追溯字段;第 5 章补入 DeepSeek 调用约束、参数、保存字段和失败处理 | 完成 |
| 第六章设计与实现逻辑混杂 | 已处理 | 正文第 6 章开头明确“只说明支撑方法复现所需的系统架构、权限控制和关键数据流”,表清单移至附录 B | 完成 |
| 系统架构不详细 | 已处理 | `user-guide-assets/17-system-architecture.png`;正文 6.1 描述前端、后端、数据库、嵌入服务、DeepSeek 和审计层 | 完成 |
| 功能模块不清楚 | 已处理 | 正文 6.2 补入“功能链路-用户操作-后端服务-核心数据表-第七章验证材料”映射;截图包含项目工作台、系统管理、实验笔记、资料库、知识图谱、智能生成和审计日志 | 完成 |
| 数据库设计缺失 | 已处理 | 正文 6.3 和附录 B.1;覆盖用户、项目、笔记、文件、图谱、RAG、AI 日志、审计等表 | 完成 |
| DeepSeek API 说明不具体 | 已处理 | 正文 5.3 写明 `/chat/completions` 非流式调用、180 秒超时、429/5xx/传输异常最多 3 次尝试、两类任务参数和日志字段 | 完成 |
| 知识图谱构建过程不完整 | 已处理 | 正文 4 章、6.7、7.4;附件 `kg-relation-gold-audit-53.csv`;图谱构建流程图和可视化截图存在 | 完成 |
| 缺少知识图谱可视化 | 已处理 | `user-guide-assets/06-knowledge-graph.png`;正文 7.4 写明图 7-1 展示实体统计、关系网络、筛选器和详情面板 | 完成 |
| 原始实验数据展示不清楚 | 基本处理 | 正文 7.2 补入从构造记录、资料库、图谱数据、关系核验、问答实验到盲评准备的处理路径表 | 基本完成,但真实课题组原始数据无法提供 |
| 系统解决的问题不清楚 | 已处理 | 正文 1.3、1.4、3 章、7.5、7.8 将问题收束为审核约束、项目权限、证据覆盖和可回放问答 | 完成 |
| 普通检索与图谱增强对比不足 | 已处理 | `rag-experiment-4.csv`;正文 7.5 报告 40 条日志、证据覆盖、任务完成率、时延和失败机制 | 完成,但只限单项目探索性结果 |
| 系统功能看起来过少 | 已处理 | 正文 6 章和截图资产覆盖用户、项目、笔记、资料、审核、图谱、RAG、评价、生成、审计 | 完成 |
| 智能体设计缺失 | 已处理但降调 | 正文不再夸大为开放自主智能体,改为固定任务型智能辅助生成;5.5、6.8、7.6 有输入、输出、来源和风险说明 | 完成 |
| 论文显得不落地 | 已处理 | 已加入真实系统截图、数据库表、API 日志、实验 CSV、图谱统计、测试结果和错误案例 | 完成 |
| 缺少系统测试部分 | 已处理 | 正文 7.3;本次实测后端 `105 passed`;前端 `npm run lint` 与 `npm run build` 通过 | 完成 |

## 三、本次实际修正

本次核查期间做了以下可执行修正:

1. `backend/app/services/knowledge_graph.py`
   - 新增集合型问题关键词判断。
   - 对“哪些、全部、所有、列出、多少、分别、归纳、汇总、清单”等集合型意图,图谱上下文上限从默认 `rag_graph_top_k` 放宽到至少 30。
   - 目的:修复 Q15 暴露的“15 条笔记被 top-10 结构性截断”问题。

2. `backend/tests/test_knowledge_graph_api.py`
   - 新增 `test_collection_query_expands_graph_context_limit`。
   - 构造 15 条 `has_note` 关系,验证“当前项目包含哪些已审核实验笔记?”能返回不少于 15 条上下文。

3. `scripts/freeze_preregistration.py`
   - 新增预注册文件 SHA-256 冻结脚本。
   - 用于后续实验运行前冻结问题、规则、指标和文件哈希。

4. `scripts/summarize_blind_review.py`
   - 新增盲评表校验和汇总脚本。
   - 未填写准确性、可追溯性、1-5 分质量或签名时会失败。
   - 两名评审时可计算一致率和 Cohen's kappa。

5. `docs/experiments/rag-experiment-5-preregistration.md`
   - 增加“项目执行门槛”,写入冻结脚本和盲评汇总脚本命令。

6. `docs/experiments/rag-experiment-5-freeze-manifest.json`
   - 生成当前预注册方案的冻结清单。

7. `docs/毕业论文初稿.md`
   - 将后端测试结果从旧的 `29 passed` 修正为当前 `105 passed`。
   - 将 Q15/top-k 表述改为“原实验批次问题 + 项目代码已修正 + 需新批次验证”。
   - 将第三章“需求分析与问题建模”改为“需求分析与问题定义”,避免把方法设计提前放入需求章节。
   - 在 3.6 增加“关键问题-需求约束-后续处理方式-可核验材料”表,把导师指出的“空泛”问题落到证据链。
   - 在 4.1 增加知识图谱处理链路表,写清输入、处理规则、输出对象和追溯字段。
   - 在 5.3 增加 DeepSeek 调用细节表,写清调用场景、提示约束、输入来源、生成参数、保存字段和失败处理。
   - 在 6.2 增加功能模块映射表,把用户操作、后端服务、核心数据表和第七章验证材料对应起来。
   - 在 7.2 增加实验材料处理路径表,明确构造数据可以验证什么、不能替代什么。

8. `docs/论文对抗式自审_20260615.md`
   - 同步更新集合查询状态:已做项目级修正,但仍需新实验。

9. `docs/审核意见/20260615-逐条回复与修改对照表.md`
   - 同步更新 Q15 回应,避免把原实验缺陷误写成当前代码仍未处理。

## 四、验证结果

本次实际运行:

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 后端全量测试 | `python -m pytest` 于 `backend` | 105 passed |
| 知识图谱专项测试 | `python -m pytest tests\test_knowledge_graph_api.py` 于 `backend` | 6 passed |
| 预注册脚本自检 | `python scripts\freeze_preregistration.py --self-test` | passed |
| 盲评脚本自检 | `python scripts\summarize_blind_review.py --self-test` | passed |
| 现有盲评表校验 | `summarize_blind_review.py --allow-incomplete` | 正确识别 40 行全部未签核 |
| 前端类型检查 | `npm run lint` 于 `frontend` | passed |
| 前端生产构建 | `npm run build` 于 `frontend` | 清理旧 `.next` 后 passed |
| 论文一致性检索 | `rg "问题建模|关键问题建模|需求分析与问题建模|stored_files|29 passed"` | 主稿无残留 |

说明:

- 首次 `npm run build` 失败于 `.next/dev/types/routes.d.ts`,属于旧 Next.js 生成目录污染。删除 `frontend\.next` 后重新构建通过。
- 这和此前项目匹配度报告里的判断一致:遇到 `.next/dev/types` 相关错误时先清理 `.next`。

## 五、仍未完成但不能自动完成的事项

以下事项不是“摸鱼未做”,而是没有人工或真实数据时不能诚实完成:

1. 独立人工盲评签核
   - 现有 `rag-experiment-4-blind-review-sheet.csv` 仍为空。
   - 脚本已确认 40 行均未完成。
   - 不能把规则化任务完成率写成人工准确率。

2. 真实多项目外部验证
   - 用户已说明真实数据无法提供。
   - 因此不能完成真实课题组、多项目留出测试、重复生成和用户研究。
   - 论文必须继续写成探索性机制分析,不能写成稳定泛化结论。

3. 强基线复现实验
   - 实验 5 预注册方案已写并冻结。
   - 但纯 LLM、BM25、直接结构化查询、当前 RAG、图谱增强 RAG 的完整多基线重跑尚未执行。
   - 没有真实数据时不应伪造结果。

4. 学校封面和签名
   - 需要真实身份字段、导师/作者信息、声明和授权书签名。

5. 查重前降重复
   - 适合最终定稿后处理。
   - 现在过早降重容易破坏已经校准的技术表述和证据边界。

## 六、当前风险和建议

### 风险 1:Markdown 与 Word 审阅版可能不同步

`docs/毕业论文初稿.md` 已在本次核查中更新,但 `docs/毕业论文_导师审阅版.docx` 在本次操作前已经是 Git 脏文件。为避免覆盖用户已有修改,本次没有直接重生成 Word。

建议:

- 如果确认 Word 审阅版可以被 Markdown 覆盖,再运行现有转换流程重生成。
- 如果 Word 里已有手工改动,应先比对再合并。

### 风险 2:新增项目修正不能回填旧实验结果

集合型图谱上下文上限已修正,但原始实验 4 的日志仍是在旧配置下生成。正文现在已经明确:

- 原批次 Q15 暴露 top-10 限制。
- 项目代码已做修正。
- 该修正不能回填为原实验结果。
- 后续要通过预注册新批次验证。

这是正确口径。

### 风险 3:导师若追问“都改完了吗”

建议回答:

> 原始导师反馈里关于结构、设计、系统实现、数据库、知识图谱、API、截图、测试和对比实验材料的可执行修改已经完成。现在剩下的是独立人工盲评、真实多项目数据和学校提交格式这些外部事项。论文当前不能宣称方法已充分验证,只能定位为可审计 ELN 问答原型与探索性机制分析。

## 七、最终判断

按 `论文导师反馈汇总.md` 的原始意见:

- 可由论文写作和项目代码完成的工作:已完成。
- 可由脚本和测试验证的支撑材料:已补齐并通过。
- 不能由作者或自动脚本代替的事项:仍未完成,并且已经在论文中明确标记。

当前最重要的不是继续加功能,而是避免把未完成的人工评价和真实数据验证写成已完成。
