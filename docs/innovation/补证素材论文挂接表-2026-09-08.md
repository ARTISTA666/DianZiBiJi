# 补证素材论文挂接表（2026-09-08，ZC-05 交付物）

> status: current
> owner: 证据线（写者 ZCode；**读者为文字线与 Codex**——本表只供取材，不构成主稿修改授权）
> 依据：`docs/experiments/innovation-supplement-2026-09-08.md`（零调用补证批次，T-024）。
> 用途：把补证批次产出的**可引用数字**与**口径红线**逐条映射到主稿《毕业论文重构稿.md》的候选落点，供文字线在窗口内取用；红线=与消融报告勘误版（ZC-01/ZC-02）、Codex 口径基线一致。

## 0. 总原则（文字线取材前必读）

1. 本表所有数字**已在运行库可复核**（SQL/审计锚点见补证报告"可复核性"节），但均属**机制层/行为层**证据，**没有一条支持效果主张**（"提升了 X""优于 Y"不写）。
2. 每条素材的"不能说"列是硬约束；越界句进入主稿前由证据线 `/thesis-review` 复审拦截。
3. 挂接位置为**候选建议**，最终落点与行文由文字线裁量；是否入稿由作者/Codex 决定。

## 1. 逐条挂接

| # | 素材（数字+出处） | 候选落点（主稿章节） | 能说 | 不能说 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| M1 | 预警四维指标 SQL 独立重算 **4/4 一致**（review_stall 0、return_rate 0.1、stagnant 定义一致、progress_deviation 0.9333）；判定逻辑与默认阈值核对通过（补证报告 §②-D） | 7.x 系统实验：预警"功能验证"小节（对应 ZC-03 ②-D D1） | 指标计算可由 SQL 独立复现，评估逻辑与留痕一致（机制正确性） | 任何"预警有效/改变行为"表述（D2 无数据）；样本仅单项目 4 指标单时点 | 高（零成本入稿，堵"指标算得对不对"一问） |
| M2 | 阈值单位不一致真实缺陷：百分数输入（80/95）vs 0-1 指标 → 告警静默失效，audit id 901/902/903/914 留痕为证；后续修复（补证报告 §②-D 末） | 8.3 限制（一段带过，"发现-定性-修复"句式）或**暂不入稿** | 系统在试运行中暴露并修复了阈值口径缺陷（体现可审计性价值的实例） | 当作"成功案例"渲染；细节表名/审计 id 入正文（报告引用即可） | 中（若作者愿意保留诚实性实例则入 8.3，否则留在实验报告层） |
| M3 | 建议→审核事后追溯：严格口径 **1/15**（系统精确匹配）、宽松口径 **3/15**（人工语义映射：Western Blot/CD63/TSG101），首条建议时点 coverage=0/15 留痕（补证报告 §②-C） | 7.x：建议行为证据小节（对应 ZC-03 ②-C C1），**与样本边界同句出现** | 建议产出后学生 14 分钟内完成 2 条审核，蓝图覆盖 0→0.067；1/15 严格命中、3/15 语义命中 | 因果表述（"建议促使学生做了 X"）；采纳率/百分比；不提 C2（前端未接线，Q-12 未完成前不存在采纳留痕数据） | 高（是三贡献②目前唯一的行为层数据，但必须带边界） |
| M4 | 蓝图实体归一化粒度发现：试剂"CD63"（蓝图）vs"anti-CD63 抗体"（实证侧）规范名不同 → 精确匹配未命中（补证报告 §②-C 表） | 7.9 失败边界/8.4 展望（作为覆盖判定的已知局限） | 覆盖判定采用 (类型,规范名) 精确匹配，试剂命名变体（"CD63"vs"anti-CD63 抗体"）会低估覆盖，列入改进方向 | 称之为"错误"（是口径定义使然，非缺陷级）；量化主张 | 中 |
| M5 | 蓝图增量解析留痕：首次 13 节点/12 边 → 二次仅补 2/2，总 15/14，同 (类型,规范名) 唯一索引零冲突（补证报告 §①-A） | 5.x 蓝图机制节（对应 ZC-03 ①-A） | 三态合并（新增/覆盖/保持）机制存在，二次解析只补增量、不推倒重建 | 耗时数字（未记录）；"幂等性已验证"（单样本不构成验证；A2 同文本实测未做，解析文本不落库） | 高（一句话+一个数字行，机制描述直接可用） |
| M6 | 消融实验 A 引用绑定表：B1 蓝图臂 3/3/0 vs B2 通用臂 0/0/0（可执行引用/命中待实证/编造）；B3/B4 未采集（补证报告 §①-B，源 JSON 未动） | 7.x 或 5.x：建议生成协议小节（对应 ZC-03 ①-B；与 `innovation-ablation-2026-09-07.md` 实验 A 同源，**不重复计数**） | 蓝图输入下兜底建议 3 条引用全部绑定真实待实证知识点且零编造，通用臂 0 条（引用绑定与覆盖指引计数对照） | 质量主张（"建议质量更高"）；比率（分母为零）；B3 强基线缺口隐去 | 高（本就计划入稿，本表只是把 B4 不可行结论补上） |
| M7 | 采纳留痕基础设施已就位（表 `agent_suggestion_feedback`、migration 0017、schema v6）但**前端未接线、无数据** | 8.4 展望（一句带过） | 采纳/忽略留痕机制已在后端就位，待前端接线后积累行为数据 | 把"表已建"说成"已上线"或"已采集"；任何采纳率数字 | 低（可不入稿，防验收人问起时口径混乱） |

## 2. 与既有稿内数字的关系核对（防冲突）

- M1 与主稿 7.x 预警段（若有）：不引入新指标值，只引入"可复算"方法论，与既有 72h/0.3 阈值叙述不冲突。
- M3 的 coverage 0.067 与演示数据 1/15 一致，与《创新点试用展示-2026-09-08.md》口径相同。
- M6 与 `docs/experiments/innovation-ablation-2026-09-07.md` 实验 A 勘误版完全同源（3/0/0 数字未重算），引用时指向同一 JSON，避免出现两套引用源。
- M7 与 ZC-05/Q-12 状态绑定：**若 Q-12 完成接线并入稿，须同步替换此行表述**。

## 3. 变更记录

- 2026-09-08 建立（ZC-05 轮）；7 条素材全部来自 T-024 补证批次，零 LLM 调用产出。
