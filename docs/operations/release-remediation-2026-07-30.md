# 发布整改状态（2026-07-30）

本页记录当前工作区从“论文支撑版”推进到可冻结发布候选版的实际状态。任何门禁只有在证据文件由真实执行产生、校验通过并绑定干净 Git 提交后才可标记完成。

## 已完成

- Next.js 16 已改用 ESLint flat config；`npm run lint`、`npm run typecheck`、`npm run build` 均通过。
- `tmp/`、`output/`、`.qoder/`、`.reasonix/` 等生成物或工具元数据已退出版本控制，本地文件未删除。
- Rust 1.88 生产后端镜像构建通过。
- Rust `cargo fmt --all --check` 与 `cargo clippy --locked --all-targets --all-features -- -D warnings` 通过。
- 门禁脚本相关测试 97 项通过。
- 陈旧内部门禁和最终门禁已重新生成，当前均明确为 `FAIL`，不再沿用旧 PASS。

## 当前代码冻结前置

1. 将 Rust 后端迁移、数据库迁移、前端重构、实验数据与论文材料按独立提交拆分。
2. 在一次性 PostgreSQL 测试库上运行：

   ```bash
   cd backend
   cargo test --locked --all-targets --all-features -- --test-threads=1
   ```

3. 运行 legacy Python、证据脚本、前端与隔离 E2E 全套检查。
4. 确认工作区干净后，重新导出系统证据并冻结 manifest；不得从脏工作区冻结。
5. 使用冻结提交的 40 位 Git revision 设置 `APP_REVISION`，再重新运行内部门禁。

## 最终成熟门禁外部事项

以下项目不能由代码或短时本机测试代替：

| 事项 | 完成标准 | 当前状态 |
| --- | --- | --- |
| 生产配置 | 对真实生产 env 执行检查，关键项全部通过且证据哈希一致 | 未完成；当前为非生产配置 |
| 外部确认性评审冻结 | 独立人员冻结项目、问题、方法、评价人及语料清单 | 未完成 |
| 长时稳定性 | 接近生产参数运行至少 4 小时、至少 1000 请求、零错误、p95 ≤ 2000 ms | 未完成；短 smoke 不计 |
| 真实 TLS | 对实际 HTTPS 域名、证书链、重定向与 HSTS 生成验证证据 | 未完成 |
| 异地加密备份 | 真实远端副本、加密、保留策略及恢复演练哈希闭环 | 未完成 |
| 最终证据清单 | 上述证据齐全后生成并验证 SHA-256 manifest | 前置未满足 |
| 人工评审完成 | 两名独立评价人完成 method-masked 评分并通过完成门禁 | 尚未启动 |

## 推荐执行顺序

代码冻结与全套 CI → 内部门禁 PASS → 生产配置与真实部署 → 4 小时 soak 与异地备份 → 外部题集/评价人冻结 → 最终成熟门禁 PASS → 正式人工评审 → 人工评审完成门禁。

