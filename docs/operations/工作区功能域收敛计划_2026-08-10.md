# 工作区按功能域收敛计划（2026-08-10）

> status: superseded
> owner: AI 优化会话
> superseded-by: 已由 2026-09-08 scripts 分域重构（T-021）实现；切片顺序仍可参考

## 当前边界

- 基线：`master` / `a215cc4`，与 AI 工作交接一致。
- 当前工作区包含至少 83 个已跟踪改动文件及多组未跟踪资产；本计划不执行 `git add`、`git commit`、重置或恢复。
- `frontend/src/app/(dashboard)/projects/[id]/system-test/page.tsx` 的删除是既有产品决策，应随前端产品收敛切片保留，不得单独恢复。
- `git diff --check` 于 2026-08-10 通过。

## 建议切片顺序

以下顺序按“先 AI/RAG，再核心业务/前端体验，最后证据与部署脚本”组织。实际暂存前必须逐文件、逐 hunk 复核；同一文件包含多个功能域时使用交互式暂存，不应整文件盲加。

### A1. 生产嵌入与 RAG 运行时契约

目标：固定 Rust 生产嵌入、schema 兼容、RAG 模式和敏感项目外发边界。

候选范围：

- `.env.example`
- `backend/src/config.rs`
- `backend/src/db.rs`
- `backend/src/embedding.rs`
- `backend/src/main.rs`
- `backend/src/state.rs`
- `backend/src/api/rag.rs` 中嵌入、同步、`mode_requires_dataset` 相关 hunk
- `backend/scripts/start-backend.sh`
- `backend/migrations/README`
- `docker-compose.e2e.yml`
- `docs/ai-rag-setup.md`

验证：Rust fmt、clippy、单线程测试；配置脚本测试；Compose config。若 API schema 变化，先不要在此切片混入生成文件，统一放 A3。

### A2. Agent 可见证据、资料选择与解释性

目标：只保存模型实际可见的 `[N]`/`[F]`/`[R]`，限制上下文预算，改进长文档和任务相关资料选择，并让前端解释无证据模式。

候选范围：

- `backend/src/api/agents.rs`
- `backend/app/services/prompts.py`
- `frontend/src/app/(dashboard)/projects/[id]/ai/page.tsx`
- `frontend/src/app/(dashboard)/projects/[id]/reports/page.tsx`
- `frontend/src/lib/citations.tsx`
- `frontend/e2e/system.spec.ts` 中 AI 模式和 Agent 引用相关 hunk

验证：Rust Agent/RAG 测试、legacy prompt 版本测试、前端 lint/typecheck/build、相关 E2E。

### A3. API 契约生成物

目标：把后端结构变化与生成的契约放在一个机械、可复查切片中。

候选范围：

- `backend/openapi.json`
- `frontend/src/lib/api-schema.d.ts`
- `docs/system-evidence/openapi.json` 和 `api-list.csv` 暂不放入本切片；它们属于最终证据快照。

验证：重新生成 OpenAPI 与前端类型后确认零漂移；前端 typecheck。

### B1. 核心业务权限、校验与审计

目标：收敛项目成员权限、笔记字段校验、资料审核、搜索索引、OCR 确认和请求审计。

候选范围：

- `backend/src/api/files.rs`
- `backend/src/api/mod.rs`
- `backend/src/api/notes.rs`
- `backend/src/api/ocr.rs`
- `backend/src/api/projects.rs`
- `backend/src/api/search.rs`
- `backend/src/models.rs`
- `backend/src/permissions.rs`
- `backend/src/api/mcp.rs`

风险：`backend/src/api/mod.rs` 同时包含代理头/IP 与限流调整；应单独审查为安全/运维子切片。`projects.rs` 的成员响应字段会影响前端类型，需与 A3 的生成顺序协调。

验证：完整 Rust 测试；权限拒绝路径、分页边界、字段长度和 MCP 项目访问测试重点复核。

### B2. 前端核心科研工作流与可用性

目标：收敛笔记、审批、资料、图谱、项目、审计和设置页面的交互改进，以及共享错误/空状态/分页组件。

候选范围：

- `frontend/src/app/(dashboard)/` 下除 A2 已列 AI/报告页面外的改动
- `frontend/src/components/` 下业务、共享和 UI 组件改动
- `frontend/src/lib/api.ts`、`utils.ts`
- `frontend/src/stores/project/`
- `frontend/src/app/(dashboard)/projects/[id]/system-test/page.tsx` 的既有删除
- 新增 `data/ocr-dialog.tsx`、`reject-dialog.tsx`、`upload-section.tsx`
- 新增 `empty-state.tsx`、`error-banner.tsx`、`page-pagination.tsx`、`AgentAssistant.tsx`
- `frontend/package.json`、`package-lock.json`、`next-env.d.ts`

风险：`AgentAssistant.tsx` 既涉及产品导航又涉及 Agent 行为，建议单独一个前端子切片；package lock 只应与实际依赖变更同时进入。

验证：lint、typecheck、build、用户旅程 E2E；特别核对只读成员、审核人、项目负责人和系统测试入口消失后的导航。

### C1. 真实实验与 Agent/RAG 评估资产

目标：把可重复评估工具与业务实现解耦，便于单独回滚且不影响生产运行时。

候选范围：

- `AGENT-EVALUATION.md`
- `evaluation-lab/`
- `docker-compose.evaluation.yml`
- `frontend/e2e/real-lab-acceptance.spec.ts`
- `frontend/e2e/fixtures/`
- `scripts/evaluate_rust_retrieval.py` 及测试
- `scripts/check_rag_evidence.py`、`freeze_rag_evidence.py` 及测试
- `evaluation-lab/agent-quality/gold-v1.json`
- `scripts/evaluate_agent_quality.py`、`validate_real_agent_quality.py` 及测试
- `docs/experiments/agent-quality-*`

验证：全部 `scripts/test_*.py`；JSON 解析；评估容器配置；真实提供方测试只在安全凭据存在时显式运行，不能成为默认 CI 的外部依赖。

### C2. 发布成熟度、健康探针与反向代理

目标：收敛 fail-closed 发布门禁、Rust 运行时证据、本地健康检查和代理配置。

候选范围：

- `scripts/check_production_config.py` 及测试
- `scripts/check_local_health.py` 及测试
- `scripts/export_rust_contract_evidence.py` 及测试
- `scripts/export_validation_evidence.py` 及测试
- `scripts/release_maturity_gate.py` 及测试
- `scripts/run-system-e2e.sh`
- `deploy/nginx.conf.template`
- 相关 README、上线准备计划和启动说明文档 hunk

验证：脚本测试、反向代理检查、Compose config、release gate 的通过和 fail-closed 反例。

### C3. 最终证据快照

目标：只在 A/B/C 实现切片确定后重新生成证据，避免把过期哈希和中间状态与代码混在一起。

候选范围：

- `docs/system-evidence/api-list.csv`
- `docs/system-evidence/manifest.json`
- `docs/system-evidence/openapi.json`
- `docs/system-evidence/restore-drill-latest.json`
- `docs/system-evidence/validation-results.json`
- `docs/system-evidence/validation-results.md`
- `docs/system-evidence/local-health-latest.json`
- `docs/system-evidence/rust-runtime-contract-latest.json`

验证：重新运行证据导出和成熟度门禁；确认 manifest 绑定最终代码/镜像，而不是当前脏工作区的中间状态。

## 暂存前必查清单

1. 再次确认 `git status --short`，确保没有新出现的用户改动。
2. 对 `backend/src/api/rag.rs`、`backend/src/api/mod.rs`、`frontend/src/lib/api.ts`、`frontend/package-lock.json` 使用逐 hunk 复核，因为它们跨功能域。
3. 每个候选切片先跑对应检查，再生成 API 或系统证据；生成物永远跟随其来源，不领先于来源。
4. 不把 `.env`、真实回答正文、密钥、请求头或含敏感数据的临时输出加入任何切片。
5. 未经用户明确授权，不执行暂存、提交、推送或 PR 操作。
