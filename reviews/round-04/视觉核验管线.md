# 视觉核验管线（双套核验标准之一）

> status: current
> owner: 证据线（ZCode）
> 建立时间：2026-09-09。依据用户确立的**双套核验标准**：论文 PDF 每轮修改必须过两套验收，
> 只过其一不得报告"完成"。

## 0. 双套核验标准（用户 2026-09-09 口述确立）

1. **脚本核验（第一层）**：编译通过（latexmk 零 error）、diff 存在且逐项对应修改点、
   pdftotext 文本层无源码残留（`latex`/`verbatim`/`needspace` 裸串）、编号引用配对。
2. **视觉核验（第二层）**：PDF 渲染成图后在"视觉"层面正确——文字无重叠、无截断、
   题注与图同页、表头单行、编号右对齐。最终交付物是视觉状态，以这一层为准。

本会话模型无图像输入通道，视觉核验通过**渲染 + OCR + 像素簇分析**三段式间接实现（见 §2）。
OCR 无法判定的残留盲区（截图内容完整性、颜色、图形美观）列入"待用户目验清单"，不得默认通过。

## 1. 管线位置与工具

| 组件 | 路径 | 用途 |
| --- | --- | --- |
| PDF 构建目录 | `/tmp/crlt/projects/thesis-ahnu-master/` | latexmk 编译 main.tex |
| 渲染 | `pdftoppm -f N -l N -r 300 -png main.pdf /tmp/vcheck/hdN` | 300 dpi 页面 PNG |
| OCR | `/tmp/vcheck/ocr_tool <png>`（Swift+Vision，`/tmp/vcheck/ocr.swift`） | 输出 `x y w h conf text` TSV，带**像素坐标** |
| 像素簇分析 | PIL 灰度投影按行/列找暗像素簇与间隙 | 判重叠（间隙→0）与越界 |

## 2. 三段式判定法

1. **OCR 坐标扫描**：同一 y 带（±20px 视为同一行）内两段文本的 x 区间若交叠 → 重叠；
   表头/正文若出现 `latex`、`needspace` 等裸串 → 转换器 fence bug。
2. **像素簇验证**（OCR 聚合歧义时）：按行带做灰度垂直投影，相邻文字簇间隙 < 10px 视为接触。
3. **盲区登记**：OCR 文本为乱码/低置信（截图内部、图形）→ 记入"待用户目验清单"，
   在交付说明中单列页码，不得标 pass。

## 3. 每轮修改的标准流程

```
改 md/转换器 → python convert → latexmk -g
→ 第一层:grep 编译日志(0 error) + pdftotext 扫残留 + diff 对照修改点
→ 第二层:定位改动所在页 → pdftoppm 300dpi → ocr_tool 坐标扫描 → 疑点像素簇复核
→ 产出:两层各自的 pass/fail 清单(带页码) + 待目验清单
```

## 4. 已验证案例存档（2026-09-09）

- **表 6-2 表头三轮调宽**：0.10/0.11 时"来源类型"折行成"来源类/型"且与"置信度"挤压
  → 0.12 后"来源类型"单行、但"金标准判定"五字仍折行 → 0.16 后
  `置信度 来源类型 金标准判定` 同层单行，像素簇最小间隙 43px，无重叠（PDF p79）。
  终版列宽：`0.09/0.19/0.16/0.19/0.09/0.12/0.16`（转换器 `fix_table_62_widths`）。
- **式(7-1)(7-2) verbatim 残留**：双 fence bug 使 7-2 整块显示为源码；转换器
  `guard()` 双 fence 归一后，全文 OCR+pdftotext 扫描 0 残留（PDF p87）。

## 5. 盲区与限制

- OCR 对截图内部文字识别率低（斜体小字/界面图形），只能判"该图是否存在"，判不了内容对错；
- 颜色类问题（着色是否正确）完全不可判；
- `/tmp/vcheck/` 会被系统清理，`ocr.swift` 已同步仓库外备份于本文件记录，重建用
  `swiftc -o /tmp/vcheck/ocr_tool /tmp/vcheck/ocr.swift`（源码存 agent-work/scripts/）。
