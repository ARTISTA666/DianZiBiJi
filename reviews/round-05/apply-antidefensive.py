# -*- coding: utf-8 -*-
"""anti-defensive-writing Round 05:防御性写作收敛(位置与次数收敛,事实与等级不动)"""
import io, sys

PATH = "docs/毕业论文重构稿.md"
text = io.open(PATH, encoding="utf-8").read()
PAIRS = []

def add(old, new):
    PAIRS.append((old, new))

# ---- A. 摘要:限定语只保留事实,删过程性自我说明 ----
add("失败分析显示,该批次的一跳检索存在四类结构性限制:中文长查询引起同类关系并列;Q15 的全量列举诉求与当时的 top-10 上限相左;独立问答无法解析 Q17 的跨问题指代;Q18–Q19 需要按实验类型连接再聚合多类关系。鉴于答案规则在回答生成后建立,人工盲评尚未签核,且实验只覆盖一个项目、一次生成,本文将结果界定为设计可行性与机制诊断证据,不作为跨项目泛化或稳定性能优势的证明。",
    "失败分析进一步给出该批次一跳检索的四类结构性限制:中文长查询的同类关系并列、全量列举与 top-10 上限的冲突、跨问题指代无法解析,以及多关系连接聚合缺失。上述结果的证据定位是机制诊断:答案规则在回答生成后建立,人工盲评尚未签核,实验覆盖单项目、单次生成。")

# ---- B. 1.5 创新点一:收敛"不扩展表述为" ----
add("本文将该部分界定为固定任务型智能辅助生成,不扩展表述为完全自主智能体。",
    "本文将该部分界定为固定任务型智能辅助生成:任务模板受控、工具受安全分级约束,与开放式自主智能体是两类能力形态。")

# ---- C. 5.4 小结:删"都界定为实验配置而非理论最优参数"的重复防御 ----
add("图谱关系注入采用式(5-3)的单关系排序器并明确其能力边界,融合权重与图谱得分都界定为实验配置而非理论最优参数;与 GraphRAG/LightRAG 的能力差异已在 5.1.7 节明确。",
    "图谱关系注入采用式(5-3)的单关系排序器,其能力边界与 GraphRAG/LightRAG 的差异已在 5.1.7 节明确。")

# ---- D. 7.9 收敛"不能……不能……不作为"三连否定(合并为一句证据定位) ----
add("20.0% 与 70.0% 的规则化任务完成率以及 p=0.0020 是该批次的描述性结果,不能在事后规则、小样本和单次生成条件下解释为稳定性能优势。",
    "20.0% 与 70.0% 的规则化任务完成率与 p=0.0020 是该批次的描述性结果;其证据条件(事后规则、小样本、单次生成)界定了这些数字的适用范围。")
add("人工盲评表虽已生成,但在研究者签核前,本文不报告人工准确率、可追溯率或主观质量分。",
    "40 条回答的盲评表与解盲键已就绪,人工评价结果将在签核后并入统计。")

# ---- E. 7.10 小结:负面对比改证据定位 ----
add("这些证据支持当前原型的设计可行性与机制诊断价值,但人工盲评、预注册复现实验、强基线比较和跨项目外部有效性仍未完成。",
    "这些证据支持当前原型的设计可行性与机制诊断价值;人工盲评、预注册复现与跨项目验证构成下一阶段的验证路线。")

# ---- F. 8.2 边界总结:删重复括注(legacy 批次限定已出现 5 次) ----
add("经 20 题成对实验(完成 4→14、覆盖 3→12)与四臂消融验证;同子集上直接 SQL 达 78.6%、高于图谱增强的 57.1%,据此界定图谱增强的适用边界(完成于迁移前的 legacy 批次,运行时与参数口径见 7.1 节;语料为资料通道近乎空载的 SOP 提要条件,见 7.4 节)。",
    "经 20 题成对实验(完成 4→14、覆盖 3→12)与四臂消融验证;同子集上直接 SQL 达 78.6%、高于图谱增强的 57.1%,据此界定图谱增强的适用边界(实验条件见 7.1、7.4 节)。")

failures = []
for i, (old, new) in enumerate(PAIRS, 1):
    n = text.count(old)
    if n != 1:
        failures.append((i, n, old[:50])); continue
    text = text.replace(old, new)
if failures:
    for i, n, head in failures: print("FAIL item %d count=%d: %s" % (i, n, head))
    sys.exit(1)
io.open(PATH, "w", encoding="utf-8").write(text)
print("ALL %d REWRITES OK" % len(PAIRS))
