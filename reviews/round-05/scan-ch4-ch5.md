# Round-05 防御性写作扫描：第四章（L442-575）与第五章（L576-930）

扫描人：anti-defensive-writing 修订员（证据线）。只读扫描，本文件为修订方案，不改论文正文。
方法：通读两章全文 + grep 复核"不是/并非/并不/不外推/仅/只/本文不"全部命中位置，逐处定性（六类：不必要的免责 / 必要的范围条件 / 真实方法学局限 / 有用的概念对比 / 基于证据的限定 / 冗余澄清），按价值排序取前 8 处。

## 修订点（按价值排序，每处给"原句→改后句"）

### 1. L791（5.2.3）冗余澄清 + 不必要的免责——"不作为当前配置"连说两次，并替审稿人递刀

原句（前 80 字）：……可反算出 0.8/0.2 的历史融合行为;但 config snapshot 未显式冻结该权重,精确代码 revision 也未知,因此该行为标为 `INFERRED`,不作为当前系统配置。式(5-2)不是当前 Rust 默认实现,也不是本文声称的理论最优参数;加权融合与 RRF 融合两种策略的历史比较仍以各自实际工件为准。

改后：前句保留至"因此该行为标为 `INFERRED`,不作为当前系统配置。"随后改为：
> 式(5-2)描述的是该历史批次的融合行为;加权融合与 RRF 融合的历史比较以各自实际工件为准。

理由："不是当前 Rust 默认实现"与前句"不作为当前系统配置"同句群重复；"也不是本文声称的理论最优参数"无人主张、属递刀。INFERRED 标记、0.8/0.2 数字、双策略以工件为准的口径全部保留。

### 2. L890（5.3.1）"不是X而是Y"滥用——概念对比首现在 2.4 节，此处冗余澄清

原句（前 80 字）：本文中的"智能体"不是开放式自主规划智能体,而是由系统预设输入、提示模板和输出格式的固定任务型辅助生成单元,以及一套受控的工具调用运行时。所有任务单元共享项目权限校验

改后：
> 本文的"智能体"指由系统预设输入、提示模板和输出格式约束的固定任务型辅助生成单元,以及一套受控的工具调用运行时。所有任务单元共享项目权限校验、已审核数据过滤、DeepSeek 受证据约束生成和来源日志保存四个步骤,区别在于任务目标、上下文组织方式和输出模板不同。（后文不动）

理由：正面定义已完整承载"固定任务型、受控"边界；与开放智能体的场景差异 2.4 节已分析，本段末（L901）"牺牲开放智能体的灵活性"句再次覆盖，三处说一件事。

### 3. L486（4.3）消极框架可改积极——以"并不只"开头的自削式主题句

原句（前 80 字）：从功能模块角度看,系统并不只包含笔记录入和关键词检索,而是覆盖从原始记录进入、可信审核、结构化转化、智能问答到结果评价的完整链路。表 4-2 列出主要模块及其输入输出。

改后：
> 从功能模块角度看,系统覆盖从原始记录进入、可信审核、结构化转化、智能问答到结果评价的完整链路。表 4-2 列出主要模块及其输入输出。

理由："笔记录入和关键词检索"的靶子是作者自设的，删除后主张更直接，信息零损失。

### 4. L740（5.1.4）以限制开头的段落 + 同段重复限定——"并非通用"与段尾"不外推"句重复

原句（前 80 字）：该方法并非通用深度语义抽取模型:规则清晰、结果可解释,易于与审核流程和项目权限配合,适合实验笔记里常见的半结构化字段与规范化记录;但面对省略中心词、长篇自由文本、隐含关系

改后：
> 该方法以规则清晰、结果可解释为设计取向,易于与审核流程和项目权限配合,适合实验笔记里常见的半结构化字段与规范化记录;对省略中心词、长篇自由文本、隐含关系和复杂同义表达,识别能力有限。第七章对四条笔记的 53 条实际关系做金标准核验,并公开误抽样本——故图谱构建结论只限定为当前规范化演示数据上的抽取一致性,不外推到通用自然语言信息抽取能力。

理由：段尾"只限定为……不外推到……"是该边界的正式承载句（首现锚点，原文保留、不动）；段首"并非通用深度语义抽取模型"是同一件事的第二次声明，删除后段首由限制开头改为设计取向开头。具体局限清单（四类困难文本）与 53 条核验证据句原样保留。

### 5. L746（5.1.5）重复声明"不做什么"——"不进图谱"与 L744 机制段重复

原句（前 80 字）：这样,未经确认的实验记录就不会进入知识图谱。科研场景中,草稿常含错误、临时假设或未完成的实验,若提前入图,会误导后续 RAG 问答与统计分析。审核后入图谱是本文可信知识管理的核心做法

改后：删除首句，段落直接开始：
> 科研场景中,草稿常含错误、临时假设或未完成的实验,提前入图会误导后续 RAG 问答与统计分析。审核后入图谱是本文可信知识管理的核心做法,也是它区别于普通知识图谱系统的主要特征之一。

理由：L744 已完整给出"草稿/退回不抽取、仅 APPROVED 触发"机制，"未经确认不进图谱"是该机制的同义复述；保留动因句与定位句（后者是正面主张）。

### 6. L502（4.3）不必要的免责——递刀式"不是页面罗列"

原句（前 80 字）：各功能模块与后续实验材料的对应关系如表 4-3 所示。该表把设计模块与第七章可复核证据连接起来,说明模块划分不是页面罗列。

改后：
> 各功能模块与后续实验材料的对应关系如表 4-3 所示。该表把设计模块与第七章可复核证据连接起来,每个模块的划分都对应可核验的验证材料。

理由："页面罗列"是替想象中的评审预设的贬低靶；正面表述同一事实（模块划分逐项落到验证材料）。

### 7. L514（4.4）消极框架可改积极——以"并不是孤立运行的"开头

原句（前 80 字）：四条功能链路并不是孤立运行的,它们通过审核状态、项目编号和审计写入串成三条主业务流。

改后：
> 四条功能链路通过审核状态、项目编号和审计写入串成三条主业务流。

理由：连接机制本身就是主张，否定式铺垫无信息增量。

### 8. L526（4.5）冗余澄清——交叉引用用否定式"不重复展开"收尾

原句（前 80 字，取句尾）：……全部 33 张表的核心字段、主外键见附录 B 表 B-1 与表 B-2,本节不重复展开字段级定义。

改后：
> 全部 33 张表的核心字段与主外键见附录 B 表 B-1 与表 B-2。

理由：交叉引用本身已表达分工，"本节不重复展开"是多余的免责式收尾。

## 核验后确认保留、不改的句（避免误伤必要范围条件）

- **L758（5.1.7）GraphRAG/LightRAG 对比句**："该方法只返回一跳关系,不进行社区摘要、路径搜索或多跳推理……而不是复现 GraphRAG 或 LightRAG。"——方法章内首现（第一章 L53/L124 为绪论层陈述），5.4 小结以"已在 5.1.7 节明确"交叉引用指向此处，链路健康，**首现保留，无重复需收敛**。
- **L818 式(5-3) 附近能力边界句**（阈值 1.0、top-10、集合型放宽 30 条、字符预算）：机制描述，无防御成分。
- **L738 式(5-1) 三句边界**（元数据编码定性 / conf(r) 不进式(5-3) / 敏感性实验 0.5-1.0 结论）：各承载独立事实，首现单次，保留。
- **L892 "本文不对这两类的生成质量下结论"**：证据等级事实（两类模板未纳入正式验证批次），平静、单次。
- **L850 "不会使用模拟答案替代"、L480 "前端不做权限兜底"**：真实安全/诚实性保证，单次出现。
- **L793 "该判据只比较两路原始分"**、**L778 同义词表"仅用于扩展 BM25 查询词项"**：机制事实性限定，非对冲。

## 范围外提示（不在本次修改范围，供持窗方参考）

L934（5.4 小结，超出 L576-930 截止线 3 行）"式(5-1)的置信度只是来源强度标记,不进入相关性得分"与 L738 首现重复，建议文字线持窗时收敛为"能力边界见 5.1.4 节"式表述。

## 红线自检

- 证据等级事实零改动：INFERRED 标记、两类模板未验证、"不外推"范围句、legacy 口径交叉引用（L766）均只在位置与次数上收敛，事实与等级不变；
- 数字（99.05%/100%、0.5/0.7/0.9/1.0、0.8/0.2、0.7/0.3、53、357、445、33 等）、式(4-1)(5-1)(5-2)(5-3)、算法 5-1、引文编号均未触碰；
- 每处给原句→改后句，逐处可经 git diff 审计与单独回退。
