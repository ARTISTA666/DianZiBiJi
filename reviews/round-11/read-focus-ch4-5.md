# Readability&Focus — ch4-5

评审对象:`docs/毕业论文重构稿.md` L450-950(第四章总体设计、第五章关键技术与方法设计,PDF 第 39-67 页)。
依据:`.zcode/skills/qu-ai-wei/SKILL.md` + `references/pattern-catalog.md`,叠加用户两条标准(晦涩 a-e / 没重心 a-d)。
约束遵守:只读评审,未改论文;第五章"解决什么问题→方案要点→与已有工作的差异→验证证据"骨架未拆;`\texttt` 标识、参数值、公式、术语(门禁/一跳关系/RRF/置信度两档)全部保持原样;改写只在分号/句号边界拆句与前置重心,未增删事实、限定与数字。按改善幅度降序,共 12 条。

## Issues

### 1. L462|4.2 首段 — 一句塞 15 个枚举标识,一段四重心

- **Location**:L462(4.2 用户类型与能力矩阵,首段)
- **原句**:"系统的用户体系由系统级角色和项目级角色两层构成,数据库层分别落在 `users.role`(userrole 枚举:`SUPER_ADMIN`、`PI`、`GROUP_LEADER`、`PROJECT_OWNER`、`REVIEWER`、`MEMBER`)和 `project_members` 表的 `project_role` 枚举(`OWNER`、`REVIEWER`、`MEMBER`、`VIEWER`)加五个能力位(`can_read`、`can_write`、`can_review`、`can_manage`、`can_evaluate`)上。其中 PI(课题组负责人)为系统级角色……系统级权限判定由 `require_admin` 统一执行……"(整段 280+ 字,连做四件事:两层定义、PI 豁免、系统级判定范围、负责人识别)
- **问题类型**:晦涩 a(一句 150+ 字、括号套枚举两层嵌套)+ 晦涩 c(一句引入 15 个枚举值/字段标识)+ 没重心 d(一段四重心)
- **改写句**(拆句并按重心分三小段,信息不丢):
  "系统的用户体系由系统级角色和项目级角色两层构成。系统级角色落在 `users.role` 的 userrole 枚举上:`SUPER_ADMIN`、`PI`、`GROUP_LEADER`、`PROJECT_OWNER`、`REVIEWER`、`MEMBER`。项目级角色落在 `project_members` 表上,由 `project_role` 枚举(`OWNER`、`REVIEWER`、`MEMBER`、`VIEWER`)加五个能力位(`can_read`、`can_write`、`can_review`、`can_manage`、`can_evaluate`)构成。// (第二段)其中 PI(课题组负责人)为系统级角色……(原文不动)。//(第三段)系统级权限判定由 `require_admin` 统一执行……(原文不动)。"

### 2. L535|4.5 首段 — 一句约 400 字枚举五个数据域、30+ 表名

- **Location**:L535(4.5 数据库逻辑设计,首段第二句)
- **原句**:"业务状态域保存用户(`users`)、课题组(`groups`/`group_members`/`group_projects`(legacy 保留表))、项目(`projects`)、成员授权(`project_members`、`project_reviewers`)……;结构化知识域保存……;检索证据域保存……;审计实验域保存……;运行时扩展域保存智能体会话状态与工具调用安全记录(`agent_sessions`、……、`mcp_http_sessions`)。"(五组分号挤在一句;关键机制"各域通过项目编号和来源编号连接……反向定位到单条问答"被压在表名墙之后)
- **问题类型**:晦涩 a(一句 400 字、五层分号+括号嵌套)+ 晦涩 c + 没重心 a(跨域连接这一设计要点埋段末)
- **改写句**(按分号拆为独立句,末句收束不变):"业务状态域保存用户(`users`)、课题组(`groups`/`group_members`/`group_projects`(legacy 保留表))、项目(`projects`)、成员授权(`project_members`、`project_reviewers`)、笔记版本与审核动作(`experiment_notes`、`note_versions`、`note_approvals`)、文件(`files`、`file_ocr_results`)和检索物化(`search_documents`)。结构化知识域保存带 `project_id` 和来源编号的实体、关系及抽取运行(`kg_entities`、`kg_relations`、`kg_extraction_runs`)。检索证据域保存项目 RAG 数据集、资料同步、分块向量(`project_rag_datasets`、`rag_file_syncs`、`rag_document_chunks`,向量列为 `vector(1024)` 并建 HNSW 余弦索引)。审计实验域保存问答日志、评价、批量实验与生成记录(`ai_query_logs`、`ai_query_evaluations`、`ai_experiment_runs`、`agent_generation_runs`),以及通用审计流水 `audit_logs`。运行时扩展域保存智能体会话状态与工具调用安全记录(`agent_sessions`、`agent_turns`、`agent_events`、`agent_messages`、`agent_steps`、`agent_pending_actions`、`tool_execution_keys`、`mcp_personal_access_tokens`、`mcp_http_sessions`)。各域通过项目编号和来源编号连接,使第七章结果能从汇总表反向定位到单条问答和原始笔记;全部 33 张表的核心字段与主外键见附录 B 表 B-1 与表 B-2。"

### 3. L747|5.1.4 置信度段 — 关键数字(F1 99.05%、敏感性结果)埋段末,一段四重心

- **Location**:L747(5.1.4,式(5-1) 之后的论证段)
- **原句**:"把置信度实现为两档来源标注而非学习得到的分数,是有意的工程取舍:在受限 schema 场景下,结构化字段优先加规则补充的抽取已达到金标准 F1 99.05%(修复后 100%)的精度,而引入学习式抽取在当前数据规模上缺乏训练语料,且会削弱抽取结果的可审计性;其代价是对自由文本泛化不足,该局限在 7.5 节的失败案例中有明确呈现。当前检索相关性得分不把 conf(r) 放进式(5-3),所以 0.7 并不比其他取值更优。第七章的敏感性实验将抽取关系置信度分别设为 0.5、0.7、0.9 和 1.0,20 个问题的前 10 条关系都不变;真正影响图谱覆盖率的是最低相关性得分阈值。"
- **问题类型**:没重心 a(敏感性实验"前 10 条关系都不变"这一关键结果埋段末)+ 没重心 d(取舍理由、代价、与评分的关系、敏感性结果四个重心挤一段)+ 晦涩 a(第二句 120+ 字、冒号内再嵌分号)
- **改写句**(拆句分组,并给后两组一个主题句;数字与限定全部保留):
  "把置信度实现为两档来源标注而非学习得到的分数,是有意的工程取舍。在受限 schema 场景下,结构化字段优先加规则补充的抽取已达到金标准 F1 99.05%(修复后 100%)的精度;引入学习式抽取在当前数据规模上缺乏训练语料,且会削弱抽取结果的可审计性。其代价是对自由文本泛化不足,该局限在 7.5 节的失败案例中有明确呈现。// conf(r) 的取值不影响检索得分:当前检索相关性得分不把 conf(r) 放进式(5-3),所以 0.7 并不比其他取值更优。第七章的敏感性实验将抽取关系置信度分别设为 0.5、0.7、0.9 和 1.0,20 个问题的前 10 条关系都不变;真正影响图谱覆盖率的是最低相关性得分阈值。"(段首"式(5-1)不是概率估计……元数据编码"一句保留不动)

### 4. L825|5.2.4 式(5-3) 符号说明 — 一句约 300 字,"4.0 分权重最大"埋在第四组句尾

- **Location**:L825(5.2.4,式(5-3) 之后的符号说明首句)
- **原句**:"式中,$\mathcal{H}(q)$ 为查询触发的预定义关系类型提示词集合,该命中项计 3.0 分;$T(q)$ 为问题分词后的词元集合,$H(r)$ 为关系 r 的源实体标签、目标实体标签、源与目标实体类型、关系类型及其显示标签共六类小写化文本串,词元与文本串全等计 3.0 分、构成子串包含计 1.0 分;来源加成项对源实体类型为 note 的关系计 0.2 分,对来源为笔记抽取(note_extraction)的关系再加 0.3 分;角色命中项统计关系属性中 roles 列表与归一化查询(小写化后的问题串)相匹配的条目数,每命中一项计 4.0 分,是权重最大的评分项。"
- **问题类型**:晦涩 a(一句 300 字、四组分号、双层括号)+ 没重心 a("每命中一项计 4.0 分,是权重最大的评分项"是全文最该被看见的解读,却压在第四组末尾)
- **改写句**(按分号拆句,末项独立成句;公式符号与数值不动):
  "式中各符号与计分如下。$\mathcal{H}(q)$ 为查询触发的预定义关系类型提示词集合,该命中项计 3.0 分。$T(q)$ 为问题分词后的词元集合;$H(r)$ 为关系 r 的源实体标签、目标实体标签、源与目标实体类型、关系类型及其显示标签共六类小写化文本串,词元与文本串全等计 3.0 分、构成子串包含计 1.0 分。来源加成项对源实体类型为 note 的关系计 0.2 分,对来源为笔记抽取(note_extraction)的关系再加 0.3 分。角色命中项统计关系属性中 roles 列表与归一化查询(小写化后的问题串)相匹配的条目数,每命中一项计 4.0 分——这是四个计分项中权重最大的一个。"

### 5. L588|第五章导语 — 一句 160+ 字:三个节号括号加两层尾注

- **Location**:L588(第五章开篇段)
- **原句**:"第四章给出了系统的总体形态;三个子问题的方法层答案在本章展开——实验知识图谱构建与审核门禁(5.1,回答"AI 用什么知识")、融合知识图谱的智能问答方法(5.2,回答"关系型问题的证据从哪来")、固定任务型智能辅助生成及工具调用机制(5.3,回答"生成如何可控"),它们是本文在成熟技术基础上的主要设计工作,也是第七章实验评价的对象。"
- **问题类型**:晦涩 a(一句 160+ 字、三个括号嵌引语再加两个尾注分句)+ 晦涩 c(一次给出 3 个节号、3 个引语、2 个评价)
- **改写句**:"第四章给出了系统的总体形态;本章展开三个子问题的方法层答案:5.1 节实验知识图谱构建与审核门禁,回答"AI 用什么知识";5.2 节融合知识图谱的智能问答方法,回答"关系型问题的证据从哪来";5.3 节固定任务型智能辅助生成及工具调用机制,回答"生成如何可控"。三者是本文在成熟技术基础上的主要设计工作,也是第七章实验评价的对象。"

### 6. L584|4.6 小结首句 — 一句 210+ 字,括号套括号再插破折号

- **Location**:L584(4.6 本章小结,首句)
- **原句**:"本章完成了系统总体设计,其核心决策是把三个子问题的答案前置到架构层:子问题一(可追溯)由"审核后入库或入图、来源留痕、三层审计"的横向约束直接保证;子问题三(可控生成)由敏感数据外发闸门与权限两层模型(系统级 `require_admin` + 项目级五个能力位,式(4-1)形式化判定)保证;子问题二(关系证据)所需的数据结构——四个数据域加运行时扩展域、实体关系全部携带 project_id 与来源编号——在本章数据库设计中成形。"
- **问题类型**:晦涩 a(一句 210+ 字:冒号下三组分号,第二组内嵌括号,第三组插破折号)
- **改写句**(拆为四句;子问题一→三→二的顺序保持原样,若非有意强调可改回一、二、三,请作者定夺):"本章完成了系统总体设计,其核心决策是把三个子问题的答案前置到架构层。子问题一(可追溯)由"审核后入库或入图、来源留痕、三层审计"的横向约束直接保证。子问题三(可控生成)由敏感数据外发闸门与权限两层模型保证——系统级 `require_admin` + 项目级五个能力位,式(4-1)形式化判定。子问题二(关系证据)所需的数据结构在本章数据库设计中成形:四个数据域加运行时扩展域,实体关系全部携带 project_id 与来源编号。"

### 7. L769|5.2 导语路线句 — 一句 200+ 字,且"分三层"与四组分节对不上

- **Location**:L769(5.2 开篇段第三句)
- **原句**:"设计分三层:5.2.1 节给出项目级 RAG 知识库的构建与资料同步方法;5.2.2 与 5.2.3 节分别设计词法检索(BM25)和混合检索的 RRF 融合算法,保证资料事实型问题的底座;5.2.4 节定义图谱关系的打分与注入策略,把关系证据补进上下文;5.2.5、5.2.6 节给出权限约束下的问答执行流程和三层来源追溯机制,保证补进来的每一项证据都可回溯(子问题一的约束在问答层落地)。"
- **问题类型**:晦涩 a(一句 200+ 字、四组分号)+ 没重心 c 的变体:宣称"分三层",冒号后却是四组小节,读者数不出三层
- **改写句**(推荐:归并为三层,5.2.1 并入"底座"层——此归并需作者确认是否符合原意):"设计分三层。第一层是资料检索底座:5.2.1 节给出项目级 RAG 知识库的构建与资料同步方法,5.2.2 与 5.2.3 节分别设计词法检索(BM25)和混合检索的 RRF 融合算法,保证资料事实型问题的底座。第二层是图谱关系注入:5.2.4 节定义图谱关系的打分与注入策略,把关系证据补进上下文。第三层是执行与追溯:5.2.5、5.2.6 节给出权限约束下的问答执行流程和三层来源追溯机制,保证补进来的每一项证据都可回溯——子问题一的约束在问答层落地。"(若不做归并,保底方案是仅在四个分号处改句号:"设计分三层。5.2.1 节给出……方法。5.2.2 与 5.2.3 节分别设计……底座。……")

### 8. L935|5.3.3 首句 — 八字段括号插进动作链中间

- **Location**:L935(5.3.3,首句)
- **原句**:"高风险工具调用并不直接执行,而是先落入 `agent_pending_actions` 待确认队列表(记录会话、用户、项目、工具名、参数 JSON、参数哈希、幂等键与过期时间),前端向用户展示将要执行的动作并等待批准或拒绝;批准后按参数执行,拒绝则关闭动作。"
- **问题类型**:晦涩 a(一句 110+ 字;八字段括号插在"先落队列表→前端展示→批准/拒绝"主干中间,行动者与动词被打断)
- **改写句**:"高风险工具调用并不直接执行,而是先落入 `agent_pending_actions` 待确认队列表。前端向用户展示将要执行的动作并等待批准或拒绝,批准后按参数执行,拒绝则关闭动作。队列表记录会话、用户、项目、工具名、参数 JSON、参数哈希、幂等键与过期时间。"

### 9. L800|5.2.3 RRF 段 — 权重与三项微调奖励挤进一句

- **Location**:L800(5.2.3,第二句)
- **原句**:"普通查询两路权重相等(1.0/1.0);偏精确词项查询采用 0.25/0.75,并在融合时附加微调项:查询与候选的词元交集奖励(每命中一词 +0.02,上限 +0.12)、完整查询串命中正文内容 +0.15、命中文件名 +0.10。"
- **问题类型**:晦涩 a+c(一句 120+ 字,分号、冒号、括号三层;一次引入权重 + 3 个奖励值共 5 个数字)
- **改写句**:"普通查询两路权重相等(1.0/1.0);偏精确词项查询采用 0.25/0.75。偏词项查询在融合时另加三项微调:查询与候选的词元交集奖励(每命中一词 +0.02,上限 +0.12)、完整查询串命中正文内容 +0.15、命中文件名 +0.10。"

### 10. L458|4.1 外部组件句 — 一句装下两组件四个标识

- **Location**:L458(4.1,首句)
- **原句**:"除数据库与生成模型外,架构还接入两类外部处理组件:文本与图像资料的 OCR 抽取(Poppler/Tesseract 管线,结果落 `file_ocr_results` 表并要求人工校对确认),以及向量化嵌入服务(开发环境使用本地哈希替身,生产目标为 OpenAI 兼容 `BAAI/bge-m3`)。"
- **问题类型**:晦涩 a+c(一句 130+ 字,冒号下两个并列项各带两三重括号信息,一次引入 Poppler/Tesseract、`file_ocr_results`、本地哈希替身、`BAAI/bge-m3` 四个新标识)
- **改写句**:"除数据库与生成模型外,架构还接入两类外部处理组件。一是文本与图像资料的 OCR 抽取,走 Poppler/Tesseract 管线,结果落 `file_ocr_results` 表并要求人工校对确认;二是向量化嵌入服务,开发环境使用本地哈希替身,生产目标为 OpenAI 兼容 `BAAI/bge-m3`。"

### 11. L753|5.1.5 规则句 — 四条规则塞一个冒号句,内嵌破折号与括号

- **Location**:L753(5.1.5,第二句)
- **原句**:"具体规则如下:草稿创建和草稿更新阶段不自动抽取图谱——草稿可能含有错误、临时假设或未完成记录,不应作为知识来源;笔记提交后进入审核流程,审核完成前图谱保持不变;审核通过后由系统自动调用知识图谱服务抽取实体与关系(若项目配置为免审模式,提交即视为通过并触发抽取);手动触发抽取与项目图谱重建接口同样只处理已审核笔记,对未审核笔记返回错误提示。"
- **问题类型**:晦涩 a(一句约 190 字,四条规则共用一个冒号句,首条内嵌破折号、第三条内嵌括号)
- **改写句**:"具体规则如下:草稿创建和草稿更新阶段不自动抽取图谱——草稿可能含有错误、临时假设或未完成记录,不应作为知识来源。笔记提交后进入审核流程,审核完成前图谱保持不变。审核通过后由系统自动调用知识图谱服务抽取实体与关系;若项目配置为免审模式,提交即视为通过并触发抽取。手动触发抽取与项目图谱重建接口同样只处理已审核笔记,对未审核笔记返回错误提示。"

### 12. L899|5.3.1 定义句 — 40 字定语链压住中心词,两个定义对象挤一句

- **Location**:L899(5.3.1,首句)
- **原句**:"本文的"智能体"指由系统预设输入、提示模板和输出格式约束的固定任务型辅助生成单元,以及一套受控的工具调用运行时。"
- **问题类型**:晦涩 b(中心词"固定任务型辅助生成单元"前压 40 字定语链,读者读到句中才知道在定义什么);"任务单元 + 运行时"两个定义对象共用一句
- **改写句**:"本文的"智能体"指两类对象:一是固定任务型辅助生成单元,其输入、提示模板和输出格式均由系统预设;二是一套受控的工具调用运行时。"

## 通过项说明

- **第五章骨架未动**:5.1(592 需求→思路)、5.2.2(779 "向量检索擅长……却不敏感。为此系统并行维护词法通道"——问题→方案教科书式)、5.2.4(806 普通 RAG 适用→关系型问题遗漏→引入图谱)、5.3(895 "前两节解决了……本节解决第三个子问题……设计取向是……")均为主题句前置、四段式骨架完整的正面样本,本次只在其内部做第 3、4、7、9、12 条的句法修复,不拆骨架。
- **表格引导句合格(e 项通过)**:表 4-1(478,给出判定函数对应)、表 4-2(495,"输入输出")、表 4-3(511,"每个模块都能对应到第七章可复核的验证材料"——读表要点明确)、表 5-1(600,"输入、处理和输出")、表 5-4(901,"已完成验证的四类任务的设计约束")、表 5-5(916,先讲五项安全属性与三级风险再指向表格)——引导句均交代了看表注意什么,未列入问题。
- **未列入的边界项**(密度偏高但改善幅度小或属体裁惯例):L943(5.4 首段)子问题二复述句约 180 字,但小结体裁的参数复述有检索价值;L945"综合而言……五点"一句约 280 字,但五点各带标签、收束全章,如需可拆为五个短句;L529(智能体任务流)一段含固定任务流 + 运行时两句,第二句实为 5.3.2/5.3.3 预告,可接受;L489 末句"前端不做权限兜底"置于句尾,但句子短、作为安全收束语自然;L912 一段覆盖四类任务,与表 5-4 平行推进,属可接受的逐一说明。
- **流程段判定**:4.4 三条业务流(525/527/529)标识密集,但每段有"X 流:"主题标签、按时间顺序推进、负分支(退回不触发写入)单独收尾,判定可读,未列入。
- **5.2.6 三层来源追溯**(887-891):每层一段、段首层名、每段单一重心,未列入。
- **保护项核查**:全部 `\texttt` 标识、参数(k1=2.2、b=0.75、rank constant=60、0.15、top-k=6、18000/600/12、temperature=0.1、max_tokens=2200 等)、式(4-1)(5-1)(5-2)(5-3) 与算法 5-1 未动;术语"门禁/一跳关系/RRF/置信度两档"未动;F1 99.05%、53 条、357/445、40 条等数字在改写句中原样保留;L747 首句"不是概率估计……而是元数据编码"属真实的定义性排除,非对称骨架,保留。
